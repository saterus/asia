%h1 Agent Authentication

= form_tag(sessions_path) do

  %p
    = label_tag :code_name, "Code Name"
    = text_field_tag :code_name, params[:code_name]

  %p
    = label_tag :password, "Password"
    = password_field_tag :password

  = submit_tag "Authenticate"
